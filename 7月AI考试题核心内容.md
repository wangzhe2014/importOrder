# 专项考核：V2 下单流程异步事件驱动重构与全链路可观测性实战

版本：V4.0（异步事件驱动与可观测性版）  
考试时间：建议 180 分钟  
文档日期：2026/08/03  
总分：100 分

> 范围限定声明
>
> 本次考试只针对 V2「万能导入解析系统」的下单主链路重构，范围限定为：
>
> 文件上传 → 文件解析 → 规则引擎 → 数据校验 → 批量落库 → 任务进度追踪 → 错误定位 → 监控告警
>
> V3 审批流程、异常工单状态机、品控暂扣、赔付审批、跨系统 Saga 分布式事务，均不在本次考核范围内。考生请勿在 V3 审批或异常工单扩展上投入精力。本题重点考察：如何把 V2 当前同步阻塞式下单流程，重构为可支撑高并发、大文件、异步任务、批量处理、故障可定位的生产级导入链路。

---

## 一、项目背景与痛点定义

### 1.1 现状说明

团队已上线 V2「万能导入解析系统」，采用 Next.js App Router + TypeScript，部署在 Vercel。系统核心能力是：

- 上传 Excel、Word、PDF 等出库单文件；
- 通过可配置解析规则引擎，将不同格式文件解析为结构化运单数据；
- 使用 AI 辅助生成解析规则，用户确认后保存；
- 在预览页面进行校验、编辑和提交；
- 将确认后的运单数据写入数据库。

当前问题在于：下单链路仍以同步阻塞方式执行。用户上传文件后，系统在一次请求中完成解析、规则执行、校验和数据库写入，全部完成后才返回响应。

这套模式在小文件场景下可用，但在大促或批量导入场景下会迅速失效。

### 1.2 大促挑战

业务方提出：大促期间系统需要支撑 10,000 单/分钟 的导入能力。现有同步架构会面临以下问题：

| 痛点 | 具体表现 | 业务影响 |
|---|---|---|
| 同步阻塞导致请求超时 | 10,000 行数据如果同步解析、逐行校验、逐行 INSERT，即使每行只耗时 200ms，总耗时也超过 30 分钟 | Vercel Serverless 请求超时，用户无法完成下单 |
| 数据库连接池打满 | 多个用户同时上传大文件，逐行查询 SKU、逐行写库会瞬间耗尽连接池 | 后续请求排队或 504，系统雪崩 |
| 前端无进度感知 | 用户只能看到 loading，不知道已处理多少、是否失败、哪里失败 | 反复点击、重复上传、客诉增加 |
| 失败定位困难 | 批量导入部分失败时，日志只有 insert failed 或 validation error | 运维必须下载原文件人工排查，定位时间过长 |
| 错误不可聚合 | 无法统计错误类型、失败批次、慢 SQL、解析耗时 | 难以判断是文件问题、规则问题、数据库问题还是系统容量问题 |
| 缺乏链路追踪 | 上传请求、解析任务、队列 Job、数据库写入日志无法串起来 | 出问题后无法从 task_id 追到具体批次和行号 |

### 1.3 本次考核核心目标

考生需要在现有 V2 项目基础上完成一次生产级重构：

- 将同步阻塞式导入改造为异步事件驱动链路；
- 使用消息队列承接文件解析和批量入库任务；
- 使用批量校验、批量写入、分批任务控制数据库压力；
- 为每个任务、批次、行级错误提供精细化追踪；
- 建设可观测性看板，使运维能在 1 分钟内定位失败原因；
- 提供压测脚本和压测报告，自证系统达到量化指标。

### 1.4 核心原则

- 不重写 V2 业务规则：已有解析规则引擎、字段映射、AI 生成规则能力必须复用，不能另起一套硬编码解析逻辑。
- 上传即返回：用户上传后应快速拿到 task_id，后续处理由后台异步执行。
- 批量优先：SKU 校验、重复检测、数据库写入必须采用批量方式，禁止逐行查询、逐行 INSERT。
- 任务可恢复：队列任务失败后可重试，失败详情可查询，不能静默丢失。
- 观测先行：每个任务、每个批次、每个阶段耗时和错误都要可追踪。
- 量化自证：必须用 10,000 行压测文件和压测报告证明性能，不接受口头说明。

---

## 二、量化目标

| 指标项 | 目标值 | 验收口径 |
|---|---:|---|
| 上传接口响应时间 | P95 ≤ 1 秒 | 用户点击上传后，接口返回 task_id，不等待后台处理完成 |
| 下单吞吐量 | ≥ 10,000 单/分钟 | 10,000 行文件从任务创建到入库完成 ≤ 60 秒 |
| 处理单元耗时 | 由考生设计并自证 | 可按批次/分片/并发任务处理；不限制每次函数调用处理多少行 |
| 前端进度刷新 | ≤ 2 秒可见变化 | 用户能看到 processed_rows、状态、错误数变化 |
| 数据库校验性能 | 必须批量校验 | 不允许逐行查询；批量大小、缓存策略、分片方式由考生设计并用日志证明 |
| 数据库写入性能 | 必须批量写入 | 不允许逐行 INSERT；批量大小、写入方式由考生设计并用日志证明 |
| 故障定位时间 MTTD | ≤ 1 分钟 | 通过 task_id、trace_id、行号定位到批次、字段、原始值和错误原因 |
| 压测主数据规模 | ≥ 20,000 条 SKU | 用于真实模拟 SKU 主数据校验 |
| 压测文件规模 | ≥ 10,000 行运单 | 用于验收 10,000 单/分钟目标 |

说明：AI 辅助生成规则的耗时不计入 10,000 单/分钟压测指标。压测应使用已确认保存的解析规则，测试纯导入执行链路。

---

## 三、技术要求

| 项目 | 要求 |
|---|---|
| 技术栈 | Next.js App Router + TypeScript，基于现有 V2 项目重构 |
| 部署 | Vercel，提供可访问 URL |
| 消息队列 | 必须引入队列或任务系统，推荐 BullMQ + Redis / Upstash Redis；也可使用 QStash、Inngest、Trigger.dev 等，但需满足重试、状态追踪、失败记录 |
| 数据库 | PostgreSQL（Neon、Supabase 等），需建立必要索引 |
| 文件处理 | 支持现有 V2 文件类型；大文件处理不得一次性把 10,000 行全部加载进请求内存链路 |
| 规则引擎 | 复用现有解析规则体系，禁止为压测文件或指定文件写死解析逻辑 |
| 压测数据 | 提供脚本生成 20,000 条 SKU 主数据和 10,000 行 Excel 压测文件 |
| 可观测性 | 自建监控看板 + traceId 链路追踪 + 批次性能日志 |
| 安全 | API Key、数据库连接串、队列连接串必须通过环境变量配置 |
| 大模型 | 可选加分，仅可辅助错误摘要或规则生成，不得成为导入主链路成功的必要条件 |

### 3.1 Vercel 环境约束

考生设计时需考虑 Vercel Serverless 的典型限制：

- 单次请求不适合长时间阻塞；
- CPU 和内存有限；
- 数据库连接池资源有限；
- 后台长任务不应依赖用户 HTTP 请求保持连接；
- 如果 Worker 需要常驻进程，可部署在 Railway、Render、Fly.io 等平台，并在 README 中说明。

---

## 四、容量规划与性能评估

### 4.1 处理单元设计

本题不强制规定每批处理多少行，也不限制每次函数调用只能处理一个 1000 行批次。考生可以根据自己的架构自行设计处理单元：

| 设计项 | 可选方案 |
|---|---|
| 处理单元大小 | 500 行、1000 行、2000 行、动态批次、按 Sheet/分区拆分等 |
| Worker 模式 | Serverless Consumer、常驻 Worker、任务平台、单 Worker、多 Worker 并发等 |
| 调度方式 | 队列 push、队列 pull、任务平台触发、常驻进程消费等 |
| 写入策略 | 分批 UPSERT、createMany、COPY、临时表合并等 |
| 校验策略 | 批量 IN 查询、缓存预热、分片查询、本地缓存辅助等 |

唯一硬性目标是：**10,000 行全链路处理完成时间 ≤ 60 秒**，且上传接口必须快速返回 task_id，不允许让用户等待全量处理完成。

考生必须在《重构假设说明》和压测报告中说明自己的处理单元大小、Worker 并发、数据库连接控制、失败重试和容量推导。

### 4.2 阶段耗时拆解参考

| 阶段 | 操作内容 | 目标耗时 | 瓶颈类型 |
|---|---|---:|---|
| 文件解析 | 读取 Excel、Word、PDF 并拆分为可处理数据块 | 考生自测 | CPU / 文件解析 |
| 规则引擎 | 字段映射、类型转换、跨行聚合、默认值等 | 考生自测 | CPU |
| 数据校验 | SKU 批量查询、必填、格式、重复校验 | 考生自测 | DB I/O |
| 批量写库 | UPSERT、createMany、COPY 等方式写入 | 考生自测 | DB I/O |
| 进度与日志 | 更新任务进度、写性能日志 | 考生自测 | DB I/O |
| 全链路合计 | 10,000 行从任务创建到完成 | ≤ 60s | 综合 |

### 4.3 10,000 单/分钟可行性推导

考生必须提交自己的容量规划，而不是照抄固定批次模型。推导至少包含：

| 推导项 | 必须说明 |
|---|---|
| 数据量 | 压测文件不少于 10,000 行 |
| 处理单元 | 批次、分片、队列消息或其他任务单元如何划分 |
| 并发模型 | Worker 数量、单 Worker 并发、Serverless 并发或常驻进程并发 |
| 数据库压力 | 峰值连接数、单次查询量、单次写入量、索引设计 |
| 失败成本 | 单个处理单元失败后的重试范围和重复处理保护 |
| 性能结论 | 为什么可以在 60 秒内处理完成 10,000 行 |

示例：考生可以选择 1000 行/批、10 个批次、消费者并发为 2；也可以选择 2000 行/批、5 个批次；还可以采用动态分片或常驻 Worker 并发消费。这些都只是实现选择，不是考试限制。

### 4.4 关键风险与应对

| 风险 | 说明 | 推荐应对 |
|---|---|---|
| Serverless 请求超时 | 上传请求不能等待 10,000 行处理完成 | 上传接口只创建任务并返回 task_id |
| Worker 运行时间和资源受限 | 处理单元过大可能导致函数超时、内存升高或重试成本过大 | 合理设计处理单元，记录阶段耗时，必要时调整并发和批量大小 |
| 数据库连接耗尽 | 多 Worker 并发批量写库可能耗尽连接池 | 限制 Worker 并发，使用连接池，避免逐行查询 |
| 消息重复投递 | 队列至少一次投递可能导致重复处理 | 处理单元 Job 幂等，按 task_id + batch_index / shard_id / message_id 去重 |
| 部分行失败 | 不能因一行错误导致整批数据全部丢失 | 成功行入库，失败行写 import_task_errors |
| 任务卡死 | Worker 崩溃或 Job 长时间无进展 | 定时扫描超时任务并恢复或标记失败 |
| 观测缺失 | 不知道慢在哪一步 | batch_performance_log 记录阶段耗时 |

---

## 五、总体架构设计

### 5.1 重构前：同步阻塞链路

~~~mermaid
flowchart LR
    U["用户上传文件"] --> API["上传 API"]
    API --> P["解析文件"]
    P --> R["执行规则引擎"]
    R --> V["逐行校验"]
    V --> DB["逐行写数据库"]
    DB --> API
    API --> U2["返回成功/失败"]

    style API fill:#FDECEA,stroke:#C00000
    style V fill:#FDECEA,stroke:#C00000
    style DB fill:#FDECEA,stroke:#C00000
~~~

主要问题：用户请求等待全流程完成；大文件容易超时；逐行查询和写库导致数据库压力过大；失败定位困难。

### 5.2 重构后：异步事件驱动链路

~~~mermaid
flowchart LR
    U["用户上传文件"] --> API["上传 API<br/>≤ 1s 返回 task_id"]
    API --> DB[("PostgreSQL<br/>import_tasks + outbox")]
    DB --> D["Outbox Dispatcher"]
    D --> Q["Queue<br/>N 个处理任务"]
    Q --> W["Import Worker"]
    W --> FS["文件/对象存储<br/>读取原始文件"]
    W --> R["复用 V2 规则引擎"]
    W --> S["批量 SKU 校验"]
    W --> B["批量 UPSERT"]
    W --> LOG["错误明细 + 性能日志"]
    LOG --> DB
    U --> P["前端轮询 task 状态"]
    P --> DB
    DB --> OBS["监控看板 / Trace 搜索"]

    style API fill:#E6F7F5,stroke:#0FC6C2
    style W fill:#E6F7F5,stroke:#0FC6C2
    style OBS fill:#E6F1FB,stroke:#185FA5
~~~

### 5.3 核心模式

| 模式 | 要求 | 落地位置 |
|---|---|---|
| 异步任务表 | 用户上传后立即创建 import_tasks，记录总行数、处理行数、状态和 traceId | import_tasks |
| 分批/分片任务 | 将大文件拆成多个可重试、可幂等的处理单元；大小由考生设计 | import_task_batches / import_task_shards 或 Queue payload |
| Transactional Outbox | 创建任务与待投递批次事件在同一数据库事务中完成 | event_outbox |
| 批量校验 | 按处理单元收集 SKU 后批量查询主数据 | Worker |
| 批量写入 | 按处理单元使用 UPSERT、createMany 或 COPY 写库 | Worker |
| 精细化错误 | 行级错误写入错误表，支持分页查询 | import_task_errors |
| 性能日志 | 记录每个处理单元的解析、规则、校验、写入耗时 | batch_performance_log |
| 链路追踪 | traceId 贯穿 API、Outbox、Queue、Worker、DB | 全链路 |

---

## 六、功能需求

### 模块一：压测数据自动准备（强制）

必须提供脚本，例如 scripts/seed-data.ts，一键完成：

1. 创建或清理压测用 SKU 主数据；
2. 插入不少于 20,000 条 SKU 主数据；
3. SKU 编码格式建议为 SKU_00001 至 SKU_20000；
4. 为每个 SKU 生成名称、规格、单位等基础字段；
5. 生成一个不少于 10,000 行运单数据的 Excel 压测文件，例如 test-data/10000-orders.xlsx；
6. 压测文件中的 SKU 应从主数据中随机抽取，并故意插入少量非法 SKU 用于验证错误定位能力。

验收要求：

- 脚本可重复执行；
- 重复执行不会造成脏数据不可控增长；
- 数据库灌入 20,000 条 SKU 主数据应在合理时间内完成；
- README 中说明执行命令和清理策略。

### 模块二：上传即返回 + 创建异步任务

上传接口必须做到：

1. 接收文件；
2. 生成 task_id 和 trace_id；
3. 保存原始文件或可复读的文件引用；
4. 快速识别总行数或通过预扫描得到总行数；
5. 创建 import_tasks 记录；
6. 按考生设计的处理单元创建 Outbox 事件；
7. 在 1 秒内返回任务信息。

响应示例：

~~~json
{
  "task_id": "task_xxx",
  "trace_id": "trace_xxx",
  "status": "PENDING",
  "total_rows": 10000,
  "total_batches": 10
}
~~~

要求：

- 上传接口不得同步执行 10,000 行完整导入；
- 前端拿到 task_id 后进入任务进度页；
- 按钮需防重复点击；
- 相同文件重复上传是否允许，由考生自行设计，并在假设说明中说明去重策略。

### 模块三：Outbox 投递与队列入队

要求：

- 任务创建和 Outbox 写入在同一个数据库事务内完成；
- Dispatcher 轮询 event_outbox，将批次事件投递到队列；
- Outbox 记录投递状态：pending、sent、failed；
- 投递失败需重试并记录 retry_count；
- 如果服务在“任务已创建但消息未投递”时宕机，恢复后 Dispatcher 必须能继续投递；
- 重复投递同一处理单元事件时，Worker 必须幂等处理。

禁止只在接口中直接调用 queue.add，却没有任何本地可靠事件记录。

### 模块四：Worker 异步处理

Worker 消费单个处理单元 Job 时，必须完成：

1. 根据 task_id 和 batch_index / shard_id / message_id 读取待处理数据；
2. 复用现有 V2 解析规则引擎，不得为压测文件写死字段；
3. 批量执行字段映射、类型转换、默认值、跨行聚合等规则；
4. 收集当前处理单元内的 SKU，批量查询 sku_master；
5. 执行必填、电话格式、数量正数、重复外部编码等校验；
6. 成功行批量 UPSERT 到运单表；
7. 失败行写入 import_task_errors；
8. 写入 batch_performance_log；
9. 原子更新 import_tasks.processed_rows、success_rows、failed_rows，避免重复累计；
10. 所有批次完成后，将任务状态更新为 completed 或 partial_success。

状态规则：

| 场景 | 任务状态 |
|---|---|
| 全部行成功 | completed |
| 部分行失败，成功行已入库 | partial_success |
| 全部处理单元失败或系统级错误不可恢复 | failed |
| 仍有处理单元运行中 | processing |
| 等待队列处理 | pending |

### 模块五：处理单元幂等与重复处理保护

必须保证：

- 同一 task_id + batch_index / shard_id / message_id 重复消费，不会重复写入运单；
- import_task_batches、import_task_shards 或等效结构中记录处理单元状态和版本；
- 批量 UPSERT 需基于稳定业务键，例如 external_order_no + sku_code + line_no；
- Job 重试不会重复累计 processed_rows；
- 已完成处理单元再次消费时应快速返回已完成结果。

### 模块六：精细化错误记录

错误明细表必须至少包含：

| 字段 | 说明 |
|---|---|
| task_id | 所属导入任务 |
| batch_index | 第几批 |
| row_number | 文件全局行号 |
| field_name | 出错字段 |
| raw_value | 原始值，敏感字段需脱敏 |
| error_code | 错误码，如 E001 |
| error_reason | 可读错误原因 |
| trace_id | 链路追踪 ID |
| created_at | 记录时间 |

建议错误码：

| 错误码 | 含义 |
|---|---|
| E001 | SKU 不存在 |
| E002 | 必填字段缺失 |
| E003 | 电话格式错误 |
| E004 | 数量不是正数 |
| E005 | 外部编码重复 |
| E006 | 规则映射失败 |
| E007 | 数据库写入失败 |
| E008 | 文件格式不支持 |

前端要求：

- 支持按批次筛选错误；
- 支持按错误类型筛选；
- 支持分页加载；
- 点击错误行可看到原始值、错误原因、建议修复方式；
- 不允许只显示“导入失败，请重试”。

### 模块七：任务进度与结果页

任务详情页至少展示：

- 文件名；
- task_id；
- trace_id；
- 当前状态；
- 总行数；
- 已处理行数；
- 成功行数；
- 失败行数；
- 总批次数；
- 已完成批次数；
- 当前吞吐量；
- 预计剩余时间；
- 最近错误摘要；
- 数据是否降级校验；
- 可导出失败明细。

前端轮询或 SSE 均可。若使用轮询，建议 1～2 秒刷新一次，避免过高频率打爆接口。

### 模块八：监控看板

必须提供“导入监控 / 任务看板”页面，至少包含 4 个核心区域：

1. 实时吞吐量
   - 展示过去 5 分钟每分钟成功入库行数；
   - 可用折线图或柱状图；
   - 数据来自任务和批次日志真实聚合。

2. 队列积压深度
   - 展示等待处理的批次数或行数；
   - 积压超过阈值（如 5000 行）时橙色预警；
   - 队列不可用时红色告警。

3. 阶段耗时分布
   - 展示解析、规则、校验、写入阶段的 P50、P95、P99；
   - 用于判断瓶颈在文件解析、规则引擎、数据库校验还是写入。

4. 错误类型分布
   - 展示 SKU 不存在、电话错误、必填缺失、数据库冲突等错误占比；
   - 可点击跳转到错误明细。

加分表现：

- 展示慢批次 TOP 10；
- 展示失败任务趋势；
- 展示数据库连接数或队列 Worker 并发；
- 接入钉钉机器人发送告警。

### 模块九：全链路 Trace 检索

必须支持按以下条件搜索：

- task_id；
- trace_id；
- 文件名；
- 批次号；
- 行号范围；
- 错误码。

搜索结果以时间线展示：

~~~text
10:00:00  用户上传文件，生成 task_id
10:00:01  按设计创建 N 个处理单元 Outbox 事件
10:00:02  处理单元 unit_001 入队
10:00:03  Worker 开始处理 unit_001
10:00:05  unit_001 校验完成，发现 3 行错误
10:00:06  unit_001 完成批量写入
10:00:07  unit_001 完成
~~~

点击失败节点应展示：

- 批次号；
- 行号；
- 字段名；
- 脱敏后的原始值；
- 错误码；
- 错误原因；
- 所属规则；
- 阶段耗时；
- 是否已重试；
- 下一步建议。

目标：运维无需下载原始文件，即可在 1 分钟内定位“第几行、哪个字段、为什么失败”。

### 模块十：容灾降级

至少实现 SKU 校验依赖异常时的降级能力：

- 当 SKU 主数据查询超时超过 3 秒，或数据库连接短暂失败；
- 系统可以进入降级模式；
- 降级模式下跳过 SKU 主数据校验，仅做本地格式校验；
- 任务详情页必须明确标注：

~~~text
⚠️ SKU 校验已降级：本次导入未经过商品主数据完整校验，数据可能需要后续复核。
~~~

要求：

- 降级不能静默发生；
- 降级状态需写入任务记录和监控日志；
- 降级后仍需记录哪些行未经过 SKU 校验；
- 服务恢复后，新任务应自动恢复正常校验；
- 是否对降级任务做后续补校验，由考生自行设计并在假设说明中说明。

### 模块十一：《重构假设说明》文档（强制）

必须随源码提交，至少包含：

1. 为什么选择异步事件驱动，而不是继续同步处理；
2. 处理单元大小如何设计，为什么这样设计；
3. Worker / Consumer 的容量规划；
4. 10,000 单/分钟的性能推导；
5. 数据库连接池和 Worker 并发如何控制；
6. Outbox 如何避免“任务创建成功但消息丢失”；
7. 处理单元 Job 如何做到幂等；
8. 部分行失败时为什么允许成功行继续入库；
9. SKU 校验降级的触发条件和风险提示；
10. 错误日志中敏感数据如何脱敏；
11. 压测数据如何生成、如何清理；
12. 如果可以向产品经理或运维团队提问，你会问哪些问题。

---

## 七、数据模型

### 7.1 复用表

| 表名 | 说明 |
|---|---|
| waybills | V2 已有运单主表，可按需增加索引，但不应破坏已有字段语义 |
| parse_rules | V2 已有解析规则表，继续复用 |

### 7.2 新增表（至少）

| 表名 | 用途 | 关键字段 |
|---|---|---|
| sku_master | SKU 主数据，用于压测和校验 | id, sku_code unique, name, spec, unit, created_at |
| import_tasks | 导入任务主表 | id, file_name, status, total_rows, processed_rows, success_rows, failed_rows, total_batches, trace_id, degraded, created_at, completed_at |
| import_task_batches / import_task_shards | 处理单元状态表 | id, task_id, unit_id, batch_index/shard_id, start_row, end_row, status, retry_count, locked_at, completed_at |
| import_task_errors | 行级错误明细 | id, task_id, unit_id, batch_index/shard_id, row_number, field_name, raw_value, error_code, error_reason, trace_id, created_at |
| event_outbox | 本地可靠事件表 | id, aggregate_id, event_type, payload, status, retry_count, next_retry_at, created_at, sent_at |
| batch_performance_log | 处理单元性能日志 | id, task_id, unit_id, batch_index/shard_id, parse_duration_ms, rule_duration_ms, validate_duration_ms, insert_duration_ms, total_duration_ms, status, trace_id |
| trace_events | 链路时间线事件 | id, trace_id, task_id, unit_id, event_name, event_status, message, occurred_at |

### 7.3 索引要求

至少需要：

- sku_master.sku_code 唯一索引；
- import_tasks.status, created_at 组合索引；
- 处理单元状态表的 task_id + unit_id 唯一索引；
- import_task_errors.task_id + unit_id 索引；
- import_task_errors.error_code 索引；
- event_outbox.status, next_retry_at 索引；
- batch_performance_log.task_id + unit_id 索引；
- trace_events.trace_id, occurred_at 索引；
- waybills.external_order_no 或业务去重键索引。

---

## 八、接口要求

### 8.1 上传接口

POST /api/import-tasks

职责：

- 接收文件和解析规则 ID；
- 创建任务；
- 写入 Outbox；
- 返回 task_id。

响应示例：

~~~json
{
  "task_id": "task_123",
  "trace_id": "trace_abc",
  "status": "PENDING",
  "total_rows": 10000,
  "total_batches": 10
}
~~~

### 8.2 查询任务进度

GET /api/import-tasks/:taskId

返回示例：

~~~json
{
  "task_id": "task_123",
  "status": "PROCESSING",
  "total_rows": 10000,
  "processed_rows": 6000,
  "success_rows": 5988,
  "failed_rows": 12,
  "total_batches": 10,
  "completed_batches": 6,
  "degraded": false
}
~~~

### 8.3 查询错误明细

GET /api/import-tasks/:taskId/errors?batch=4&error_code=E001&page=1&page_size=50

### 8.4 查询批次性能

GET /api/import-tasks/:taskId/batches

### 8.5 Trace 搜索

GET /api/traces/:traceId

### 8.6 监控聚合

GET /api/import-monitor/summary

返回吞吐、队列积压、阶段耗时、错误分布等聚合指标。

---

## 九、事件契约

### 9.1 事件信封

所有事件建议使用统一信封：

~~~json
{
  "event_id": "evt_123",
  "event_type": "ImportBatchCreated",
  "schema_version": 1,
  "aggregate_id": "task_123",
  "trace_id": "trace_abc",
  "occurred_at": "2026-08-03T10:00:00.000Z",
  "payload": {
    "task_id": "task_123",
    "unit_id": "unit_001",
    "start_row": 1,
    "end_row": 1200
  }
}
~~~

### 9.2 至少定义的事件

| 事件 | 生产者 | 消费者 | 说明 |
|---|---|---|---|
| ImportTaskCreated | 上传 API | Dispatcher / Worker | 导入任务已创建 |
| ImportBatchCreated | 上传 API / Outbox | Worker | 批次待处理 |
| ImportBatchStarted | Worker | Trace / Monitor | 批次开始 |
| ImportBatchSucceeded | Worker | Task Aggregator | 批次成功 |
| ImportBatchFailed | Worker | Task Aggregator / Alert | 批次失败 |
| ImportTaskCompleted | Worker / Aggregator | Monitor | 任务完成 |
| ImportTaskPartialSuccess | Worker / Aggregator | Monitor | 任务部分成功 |
| ImportTaskDegraded | Worker | Monitor / UI | 任务发生降级 |

### 9.3 事件版本要求

- 事件必须包含 schema_version；
- 新增字段应向后兼容；
- 消费者必须能忽略未知字段；
- 重大字段语义变化需要升级版本；
- README 中说明事件字段变更策略。

---

## 十、测试与压测要求

### 10.1 自动化测试（必须）

至少覆盖：

- 上传接口 1 秒内返回；
- 任务创建与 Outbox 写入同事务；
- Dispatcher 可恢复投递；
- Worker 处理单元执行成功；
- Worker 处理单元重复消费幂等；
- SKU 批量校验；
- 部分行失败但成功行入库；
- 错误明细按行记录；
- 任务最终状态聚合；
- 降级模式触发；
- Trace 时间线生成；
- 权限或非法 task_id 查询保护。

### 10.2 压测脚本（必须）

必须提交压测脚本，建议使用 k6、Artillery 或自定义 Node.js 脚本。压测至少包含：

1. 使用 10,000 行 Excel 文件上传；
2. 记录上传接口响应时间；
3. 轮询任务状态直到完成；
4. 统计总耗时；
5. 校验最终成功行数、失败行数；
6. 输出是否达到 ≤ 60 秒目标；
7. 记录是否出现 500 / 504 错误。

### 10.3 压测报告（必须）

压测报告至少包含：

- 测试时间；
- 部署环境；
- Worker 数量和并发配置；
- 数据库类型和连接池配置；
- SKU 主数据数量；
- 压测文件行数；
- 上传接口 P95；
- 任务总耗时；
- 各处理单元 P50 / P95；
- SKU 校验耗时；
- 数据库写入耗时；
- 错误率；
- 数据库连接数截图或日志；
- 监控看板截图；
- 结论和已知瓶颈。

---

## 十一、考点与评分标准（总分 100 分）

### 前置红线

出现以下任一情况，本次考试直接计 0 分：

1. 未部署可访问的在线系统；
2. 未提供 20,000 条 SKU 主数据灌入脚本；
3. 未提供 10,000 行压测 Excel 文件；
4. 仍采用“上传后同步 for 循环逐行 INSERT”的主链路；
5. 为压测文件或指定文件写死解析逻辑，绕过 V2 规则引擎；
6. 提交代码中包含真实 API Key、数据库密码或 Redis 连接串。

出现以下情况，最高不超过 50 分：

- 上传接口无法在 1 秒内返回；
- 没有消息队列或等效异步任务系统；
- 没有批量校验和批量写入，仍大量逐行查询/写库；
- 没有行级错误记录，无法定位具体行和字段；
- 没有任何监控看板或 Trace 检索能力。

### 考点 1：异步事件驱动架构（20 分）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| 上传即返回 | 5 | 上传接口 P95 ≤ 1 秒，返回 task_id，不等待后台完成 |
| 任务模型 | 4 | import_tasks、批次状态、进度字段设计清晰 |
| Outbox 可靠投递 | 5 | 任务创建和批次事件同事务写入，宕机后可恢复投递 |
| 队列/Worker 架构 | 4 | 处理单元 Job 正确入队、消费、重试，Worker 边界清晰 |
| 状态流转 | 2 | pending、processing、completed、partial_success、failed 状态准确 |

### 考点 2：批量处理与性能达标（25 分，核心）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| 异步/分片处理 | 5 | 10,000 行脱离上传请求异步处理；处理单元划分合理，不限定批次大小 |
| 复用规则引擎 | 4 | 复用 V2 解析规则，不硬编码文件格式 |
| SKU 批量校验 | 5 | 禁止逐行查询；批量查询策略合理，并提供真实耗时日志 |
| 批量写入 | 5 | 禁止逐行 INSERT；批量写入策略合理，并提供真实耗时日志 |
| 10,000 单压测 | 6 | 总耗时 ≤ 60 秒，无 500/504；超过 60 秒本项最高 3 分 |

### 考点 3：幂等、重试与任务恢复（15 分，核心）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| 处理单元幂等 | 5 | 同一 task_id + unit_id/message_id 重复消费不重复入库、不重复累计进度 |
| Job 重试 | 3 | 可重试错误有重试次数和错误记录 |
| 卡死恢复 | 3 | 长时间 processing 的批次可被恢复或标记失败 |
| 部分成功处理 | 2 | 部分行失败不影响成功行入库，状态为 partial_success |
| 重复上传策略 | 2 | 对重复文件/重复任务有明确策略和说明 |

### 考点 4：错误精细化与用户可修复性（10 分）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| 行级错误表 | 4 | 包含批次、行号、字段、原始值、错误码、原因 |
| 错误查询体验 | 3 | 前端支持按批次/错误码筛选和分页 |
| 错误可解释 | 2 | 错误原因可读，能指导用户修复 |
| 敏感数据处理 | 1 | 手机号、地址等敏感字段脱敏展示和记录 |

### 考点 5：全链路可观测性（20 分，核心）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| traceId 穿透 | 5 | traceId 贯穿上传 API、Outbox、Queue、Worker、DB 日志 |
| 监控看板 | 6 | 展示吞吐、队列积压、阶段耗时 P99、错误分布 |
| Trace 搜索 | 4 | 可按 task_id、trace_id、行号定位到批次和失败节点 |
| 性能日志 | 3 | 每个处理单元记录解析、规则、校验、写入、总耗时 |
| 告警能力 | 2 | 队列积压、死信/失败任务、耗时异常有明显提示或通知 |

### 考点 6：容灾降级与容量规划（5 分）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| SKU 校验降级 | 2 | DB 查询超时可降级且前端明确提示风险 |
| 容量推导 | 2 | 文档给出处理单元划分、Worker 并发、连接池和吞吐推导 |
| 清理策略 | 1 | 说明压测数据、错误日志、Outbox、性能日志的清理/归档 |

### 考点 7：提交质量与工程规范（5 分）

| 评分要点 | 分值 | 细则 |
|---|---:|---|
| README 完整 | 2 | 本地启动、环境变量、部署、压测、验收步骤清晰 |
| 测试覆盖 | 2 | 自动化测试覆盖关键链路 |
| 代码质量 | 1 | TypeScript 类型清晰，模块边界合理，无明显密钥泄漏风险 |

---

## 十二、评分等级对照

| 职级 | 合格线 | 说明 |
|---|---:|---|
| 资深工程师 | 90 分 | 异步架构、批量性能、幂等恢复、可观测性基本不丢分；能解释容量推导与生产风险 |
| 高级工程师 | 80 分 | 核心链路完整，性能基本达标；允许少量监控或降级细节缺漏 |
| 中级工程师 | 70 分 | 能完成上传异步化、分批处理、批量写库、错误明细和基础监控 |
| 初级工程师 | 60 分 | 能完成任务化导入、队列处理和基础进度展示；性能与可观测性可部分缺漏 |

---

## 十三、提交物清单（强制）

1. 在线地址：Vercel 可访问 URL；
2. 源码仓库：GitHub / GitLab / Gitee；
3. 压测数据脚本：生成 20,000 条 SKU 主数据；
4. 10,000 行压测 Excel 文件；
5. 压测报告：证明 10,000 行总耗时 ≤ 60 秒；
6. 架构设计文档：包含异步任务流程图、Outbox、批量处理策略；
7. 《重构假设说明》：覆盖第六章模块十一要求；
8. 接口文档：上传、任务查询、错误查询、Trace 查询、监控聚合；
9. README：本地启动、环境变量、部署、压测、故障模拟说明；
10. 演示账号或访问说明：可查看导入页、任务页、监控页。


---

## 十四、反思题（0 分，不计分）

1. 为什么 V2 下单导入链路不能继续采用同步阻塞方式？什么时候同步反而更简单可靠？
2. 处理单元变大或变小，会分别带来什么影响？
3. 如果吞吐目标从 10,000 单/分钟提升到 50,000 单/分钟，最先成为瓶颈的是 Worker、Redis 队列还是数据库写入？你会如何扩展？
4. 如果队列消息重复投递，为什么“业务幂等”比“消息只投递一次”更重要？
5. 如果某个处理单元中有部分行失败，你认为应该整体回滚还是成功行先入库？为什么？
6. 错误明细中需要保留原始值，但手机号和地址属于敏感信息，你如何平衡排障效率和数据安全？
7. 如果 Outbox 表持续增长到千万级，你会如何清理、归档和设计索引？
8. 如果 AI 生成的解析规则导致大量字段映射错误，监控系统如何帮助快速发现是规则问题而不是数据库问题？

---

## 十五、考试纪律声明

本次考试中，若不同考生作品出现以下情况之一，视为作弊，本次考试成绩计 0 分：

- UI 高度相似，且非公共组件或统一设计规范导致；
- 代码结构、命名、注释、压测脚本高度一致；
- 存在相同非预期 BUG 或相同错误处理缺陷；
- 压测报告、截图、监控数据高度相似或明显伪造；
- 《重构假设说明》内容高度相似；
- 为通过验收而硬编码压测文件名、固定行号或固定 SKU。

允许使用 AI 辅助编码和文档整理，但考生必须能解释核心设计，并能根据阅卷人现场修改要求调整实现。无法解释异步任务、批量处理、幂等、Outbox 或可观测性设计的，相关考点不得分。
