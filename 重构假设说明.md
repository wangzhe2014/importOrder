# V2 下单流程异步事件驱动重构假设说明

## 1. 为什么采用异步事件驱动

上传请求只负责接收文件、预扫描、落盘和创建任务，不等待解析、校验和写库。这样可以避开 Serverless 请求超时，并把 CPU、数据库 I/O 和失败重试从用户请求中隔离出来。

## 2. 处理单元

默认 `1000 行/批`。Excel 标准表格按原始数据行切片；Word、PDF 和复杂结构默认作为一个处理单元。批次拥有独立状态、重试次数、锁定时间和完成时间。

## 3. Worker 容量规划

默认 Worker 并发为 2。每个批次的 SKU 使用批量 `IN` 查询，写入按 500 行分块 UPSERT。并发数通过 `WORKER_CONCURRENCY` 调整，避免数据库连接池被多个批次同时占满。

## 4. 10000 行吞吐推导

10000 行约拆为 10 个批次。理论处理时间为 `批次耗时 × ceil(10 / Worker 并发)`，实际结论必须由 `scripts/load-test.ts` 的真实运行结果给出。本仓库不把理论值当作压测结果。

## 5. 数据库连接控制

Worker 不执行逐行查询或逐行 INSERT。SKU 校验按 500 个编码分块，写库按 500 行分块，Worker 并发默认 2。生产环境应根据 Supabase/Neon 连接池监控调整并发。

## 6. Outbox 可靠投递

`create_import_task` RPC 在同一个 PostgreSQL 函数事务中写入 `import_tasks`、`import_task_batches` 和 `event_outbox`。Dispatcher 投递成功后再标记 `sent`。若在入队后、标记前宕机，可能发生重复投递，由批次 CAS 认领和 UPSERT 幂等处理。

## 7. Job 幂等与恢复

`claim_batch` 只允许 pending 或超时 processing 的批次被认领；`complete_batch` 从批次账本重算任务聚合值，避免重试重复累计。`recover_stale_batches` 定期恢复卡死批次。

## 8. 部分行失败

格式、SKU、重复和数据库错误按行记录。通过校验的行继续批量写入，失败行写入 `import_task_errors`，任务最终状态为 `partial_success`。这样不会因为少量坏数据阻塞整批有效数据。

## 9. SKU 降级

SKU 批量查询超过 `SKU_CHECK_TIMEOUT_MS`（默认 3000ms）或数据库异常时，任务标记 `degraded=true`，跳过 SKU 主数据存在性判断，仅执行本地格式校验。前端显示风险提示，并写入 Trace 事件。

## 10. 敏感数据脱敏

手机号保存为前三位加四位尾号，中间脱敏；地址保留前六位和后四位。错误明细 API 只返回脱敏后的 `raw_value`。

## 11. 压测数据清理

`npm run seed` 会清理 `sku_master` 中 `SKU_` 前缀的数据后重新写入 20000 条，并覆盖生成 `202607/10000-orders.xlsx`。压测任务、错误、性能、Trace 数据和对象存储中的导入原始文件应在独立环境中定期归档清理。

## 12. 文件存储

文件存储统一使用 `UPLOAD_STORAGE_BACKEND=supabase` 和 `SUPABASE_STORAGE_BUCKET=import-files`。上传 API 把原始文件保存到 Supabase Storage，Worker 通过 `supabase://bucket/task/file` 引用复读文件，避免 Vercel/独立 Worker 的本地临时盘不可共享问题。代码仍保留 `local://` 读取兼容逻辑，仅用于历史本地任务引用。

Supabase Storage 端到端回归记录见 `202607/supabase-storage-regression.json`。该回归证明生产存储链路可用；本地网络下包含远端 Storage 上传/下载，会比本地文件存储基准慢。

## 13. 需要向产品和运维确认的问题

1. 重复上传是否按文件哈希去重，还是允许创建新任务？
2. 部分成功数据是否允许直接下游使用？
3. 降级任务是否需要服务恢复后的补校验？
4. 错误原始值的保留周期和脱敏规则是什么？
5. 生产数据库连接池、队列积压和死信告警阈值是多少？
6. Supabase Storage 原始文件保留周期、bucket 权限和生命周期清理策略是什么？
