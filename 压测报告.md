# 异步导入压测报告

## 报告状态

已完成本地文件存储基准压测和 Supabase Storage 优化后回归压测。本报告只记录实测结果，不使用理论数据替代。

## 测试配置

| 项目 | 值 |
|---|---|
| 测试时间 | 2026-08-06T01:13:24.691Z |
| 部署环境 | 本地生产模式 `http://localhost:3002` |
| 队列后端 | DB fallback queue（Worker 日志：`QUEUE_BACKEND=db`） |
| Worker 数量/并发 | 1 进程 / 并发 2 |
| 批次大小 | 1000 |
| 数据库 | Supabase PostgreSQL |
| SKU 主数据 | 20000 |
| 文件行数 | 10000 |
| 文件大小 | 656917 bytes |
| 任务 ID | `task_msgtnldl_7nc6p1` |
| Trace ID | `trace_msgtnldl_qzl9qn2q` |

## 运行命令

```bash
npm run seed
npm run build
npm run start -- -p 3002
npm run worker
LOAD_TEST_BASE_URL=http://localhost:3002 npm run loadtest
```

## 结果记录

| 指标 | 实测值 | 目标 |
|---|---:|---:|
| 上传接口 P95 | 886ms | <= 1000ms |
| 任务总耗时 | 20882ms | <= 60000ms |
| 成功行数 | 9996 | 与最终任务一致 |
| 失败行数 | 4 | 记录 E001 等错误 |
| 500/504 数量 | 0 | 0 |
| 批次耗时 P50/P95 | 9805ms / 10965ms | 记录日志 |
| 解析耗时 P50/P95 | 3417ms / 7530ms | 记录日志 |
| SKU 校验耗时 P50/P95 | 29ms / 52ms | 记录日志 |
| 批量写入耗时 P50/P95 | 1382ms / 1700ms | 记录日志 |
| 队列积压 | 0 批次 / 0 行 | 无持续积压 |

## 错误分布

| 错误码 | 数量 | 说明 |
|---|---:|---|
| E001 | 4 | 压测文件中预置的非法 SKU 行，任务最终状态为 `partial_success` |

## 结论

本地 DB fallback Worker 在 20882ms 内完成 10000 行处理，成功 9996 行、失败 4 行，队列最终无积压，满足 60 秒内完成 10000 行处理的目标。

上传接口 P95 实测 886ms，达到 1000ms 目标。当前上传请求只做流式文件接收、落盘、创建空任务和投递异步规划事件；行数统计、批次拆分和批次投递由 Worker 异步完成。压测 Excel 使用 xlsx 压缩写入，保留 10000 行和 10 列，文件大小为 656917 bytes。

原始压测结果文件：`scripts/loadtest-report-loadtest_1785978783809.json`。

## Supabase Storage 优化回归

### 优化内容

- 规划阶段只下载并解析原始上传文件一次。
- 规划阶段生成 `${task_id}/_planned.json.gz` 中间产物，保存按 `unit_id` 切好的解析结果和任务级 SKU 校验输入。
- 批次处理优先复用 Worker 内存缓存或 `_planned.json.gz`，不再每批次重复下载和重复解析原始 Excel。
- SKU 校验改为任务级校验，优先使用 `validate_sku_codes` RPC；RPC 未迁移时自动退回并发分块查询。

### 回归配置

| 项目 | 值 |
|---|---|
| 测试时间 | 2026-08-07T00:43:23.333Z |
| 部署环境 | 本地生产模式 `http://localhost:3004` |
| 存储后端 | Supabase Storage |
| 队列后端 | `auto`，本地 DB fallback queue |
| Worker 数量/并发 | 1 进程 / 并发 2 |
| 批次大小 | 1000 |
| 文件行数 | 10000 |
| 任务 ID | `task_msi80s9q_6w33bl` |
| Trace ID | `trace_msi80s9q_a497xwd0` |

### 回归结果

| 指标 | 优化前 Supabase Storage | 优化后 Supabase Storage | 目标 |
|---|---:|---:|---:|
| 上传接口耗时 | 5929ms | 5072ms | <= 1000ms |
| 任务总耗时 | 71437ms | 24772ms | <= 60000ms |
| 成功行数 | 9993 | 9990 | 与最终任务一致 |
| 失败行数 | 7 | 10 | 记录 E001 等错误 |
| 最终状态 | `partial_success` | `partial_success` | 可解释终态 |
| SKU 校验降级 | 未记录 | false | false |

优化后 Supabase Storage 路径在 24772ms 内完成 10000 行处理，较优化前 71437ms 明显下降，并重新满足 60 秒目标。上传接口仍受本地到 Supabase Storage 网络影响，未达到 1000ms；生产建议将 Web 服务和 Worker 部署到靠近 Supabase 项目区域的环境。

Supabase Storage 优化回归结果文件：`scripts/loadtest-report-loadtest_1786063378561.json`。
